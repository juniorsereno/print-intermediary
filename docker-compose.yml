services:
  print-intermediary:
    build: .
    restart: always
    environment:
      - PORT=3000
    # Volumes removed for production/Easypanel deployment
    # The code is already copied inside the image via Dockerfile
